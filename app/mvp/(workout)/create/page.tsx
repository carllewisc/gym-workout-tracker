import WorkoutViewPage from '../_components/workout-view-page';

export const metadata = {
  title: 'Dashboard : Create Workout'
};

export default function Page() {
  return <WorkoutViewPage mode="create" />;
}
